export const state = {
  // Summaries
  rolesSummaries: []
};

export const mutations = {
  // Summaries
  setRolesSummaries(state, rolesSummaries) {
    state.rolesSummaries = rolesSummaries;
  }
};
